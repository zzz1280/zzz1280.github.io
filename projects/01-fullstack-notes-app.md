# 01｜全栈待办 / 笔记应用（Todo-Notes）

> 我的第一条完整 Web 开发链路：用户系统、数据库、前后端交互、自动化测试、容器化部署，一个都不少。

| 状态 | ✅ 已完成（2026-09-10 交付） | 主线 | Web 全栈（TypeScript） | 仓库 | [zzz1280/todo-notes](https://github.com/zzz1280/todo-notes) | 难度 | ★★☆☆☆ |
| --- | --- | --- | --- | --- | --- | --- | --- |

> **交付备注（2026-09-10）**：最终实现采用 TypeScript 全栈（React 19 + Vite + Express 5 + Prisma + SQLite + JWT 双 token + Docker Compose / nginx），与立项时计划的 Flask 技术栈不同——开发过程中与 AI 结对评估后改道 TS 一门语言贯穿前后端。下方验收标准全部达成，实测记录见仓库 README。

## 一、项目背景与定位

- 所有者：朱锦琰（重庆大学 机械设计制造及其自动化 2024 级）。
- 这是我的第一个完整 Web 项目：自己天天会用的待办 / 笔记工具，用它的开发过程打通"前端 → 后端 → 数据库 → 测试 → 部署"整条链路。
- **⚠️ 作品集定位声明：本项目是用于个人主页 https://zzz1280.github.io 「项目实践」区展示的核心作品**，同时是 06（AI 增强）和 08（ESP32 数据站）两个项目的基础设施。面试时它会是我讲得最熟的项目，所以每一步都要能回答"为什么这么设计"。

## 二、成果定义（做完的标准）

**必须实现（Must）——全部达成 ✅**
- [x] 注册 / 登录 / 退出：密码 bcrypt 哈希存储，JWT 双 token 会话管理（accessToken 15 分钟 + refreshToken 7 天，401 自动静默换发），未登录路由守卫
- [x] 笔记 / 待办的增、删、改、查，数据按用户强制隔离（每个用户只能访问自己的笔记）
- [x] 数据持久化到 SQLite（Prisma ORM；Docker 命名数据卷，实测容器销毁重建数据不丢失）
- [x] 完成状态切换（乐观更新）、关键词搜索（300ms 防抖）、标签筛选与分页
- [x] 一条命令本地跑通：`docker compose up` 一键启动（见仓库 README）

**加分项（Should）**
- [x] 标签系统（多对多）与筛选芯片（优先级筛选暂未做）
- [ ] 每周完成率统计图（Chart.js）——未做，可在 06 迭代时补充
- [x] 容器化部署（Docker Compose 双容器 + nginx 反向代理；因含后端服务 GitHub Pages 无法托管，无公网演示地址——不做假入口）

**交付物清单（作品集要求）**
- [x] 公开 GitHub 仓库 https://github.com/zzz1280/todo-notes ，README 含项目简介、技术栈、本地运行方法
- [x] 两套自动化测试：27 项 API 冒烟测试（可重复运行）+ 11 项浏览器端到端测试（注册 → 建笔记 → 筛选 → 退出 → 再登录验证数据持久化），全部通过
- [ ] README 截图 / GIF（可后补：登录页、笔记主页、docker compose up 启动过程）
- [x] 个人主页项目卡片更新：状态徽章 ✅ 已完成 + 仓库链接 + 技术标签（2026-09-10 完成）
- [x] 能对着 README 和代码讲清楚：路由设计、密码为什么哈希、SQL 注入怎么防（Prisma 参数化查询）、登录态怎么维持（双 token 静默换发）

**实际技术栈**：TypeScript / React 19 / Vite / Tailwind CSS / React Router / Express 5 / Prisma ORM / SQLite / JWT（双 token）/ bcrypt / Docker Compose / nginx。

## 三、技术路线（分阶段）——已全部执行完毕

> 以下为立项时的阶段规划，实际执行以 TS 全栈落地，过程记录见仓库提交历史。

- ~~阶段 0｜现状盘点~~ → 开发过程中直接以真实代码推进。
- ~~阶段 1｜数据模型与用户系统~~ → User / Note / Tag 三表（标签多对多、删除用户级联删除），bcrypt 哈希，JWT 双 token。
- ~~阶段 2｜笔记 CRUD~~ → 列表接口支持按状态 / 标签 / 关键词筛选与分页，查询强制数据隔离。
- ~~阶段 3｜前端与体验打磨~~ → 登录注册页字段级错误提示、路由守卫、静默续登、乐观更新、防抖搜索、删除二次确认。
- ~~阶段 4｜部署上线~~ → Docker Compose 双容器：前端多阶段构建由 nginx 托管并反代 /api，后端容器启动自动迁移；已解决国内拉取基础镜像超时（构建参数化切换镜像源，默认官方源）。
- ~~阶段 5｜收尾~~ → README、测试、提交仓库、主页卡片更新、复盘。

## 四、Agent 运行规范（精简版，务必遵守）

> 项目已交付，本节规范保留作为 06 / 08 迭代时的参照：一次一个阶段、汇报等确认、不虚构完成、秘密走环境变量、SQL 一律参数化、小步提交（feat: / fix: / docs:）、每阶段更新进度日志、全程中文。

## 五、风险与注意（复盘）

- 密码存储是面试必问点：已用 bcrypt 哈希 + 盐，绝不明文 / MD5。✅
- 数据权限隔离：所有查询强制带 user_id，已作为硬性约定贯穿全部接口。✅
- 教训：SQLite 单文件在容器里必须挂命名卷，否则容器重建即丢数据——已实测验证。

## 六、主页展示要求——已完成 ✅

2026-09-10 已更新主页卡片：状态徽章 `✅ 已完成`；描述突出全链路（数据库建模 → API → 前端 → 测试 → 容器化部署）与测试数字；标签 `TypeScript / React 19 / Express 5 / Prisma / Docker Compose`；仓库链接 https://github.com/zzz1280/todo-notes 。因包含后端服务，未添加在线 demo 假入口。

## 七、进度日志

| 日期 | 阶段 | 完成内容 | 下一步 |
| --- | --- | --- | --- |
| 2026-09-10 | 阶段 0~5 全部 | Todo-Notes 交付：三表建模（多对多标签 + 级联删除）、JWT 双 token 自动续期、筛选分页、乐观更新与防抖；27 项 API 冒烟 + 11 项 E2E 测试全部通过；Docker Compose 双容器部署，数据卷持久化实测通过；解决国内镜像拉取超时 | 06 号 AI 增强（摘要 / 标签 / 语义搜索）可在此项目基础上迭代；README 截图 / GIF 可后补 |
